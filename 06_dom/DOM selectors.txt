// DOM selector
// look basic structure of dcument
 
window => document => HTML => head & body
head => title & meta tags
body => div => h1 , span , p

so head and body are parent class and tags inside them are child class 
textnode : so inside title we have written text so we call it text node 
attributes: so inside h1 or p or div we declare some attributes like class , id etc so these are attributes




<!-- document.getElementById("title")
<h1 id=​"title" class=​"heading">​Learning DOM​</h1>​
document.getElementById("title").id
'title'
document.getElementsByClassName("para")
HTMLCollection [p.para]0: p.paralength: 1[[Prototype]]: HTMLCollection
document.getElementById("title").class
undefined
document.getElementById("title").className
'heading'
document.getElementById("title").getAttribute("class")
'heading'
document.getElementById("title").setAttribute("class","test")
undefined
document.getElementById("title")
<h1 id=​"title" class=​"heading">​Learning DOM​</h1>​
const title = document.getElementById("title")
undefined
title
<h1 id=​"title" class=​"heading">​Learning DOM​</h1>​
title.style.backgroundColor = "green"
'green'
title.style.borderRadius = "20px"
'20px'
title.style.padding = "10px"
'10px' -->


// diffrence between innerText , innerHTML , textContent
<h1 id="title" class="heading">Learning DOM <span style: display: "none">hello world</span></h1>
// here we have created a span inside h1 but if we set its display to none then 
// if we use textContent it will be not visible , but if we use innerText it will be visible 
// and innerHTML will display 
title.innerHTML
'Learning DOM <span>hello world</span>'


title.innerText
'Learning DOM hello world'
title.innerHTML
'Learning DOM <span>hello world</span>'
title.textContent
'Learning DOM hello world'
title.querySelector("span")
<span>​hello world​</span>​
title.querySelector("span").style.display = "none"
'none'
title.textContent
'Learning DOM hello world'
title.innerText
'Learning DOM'


//***************************************  queryselector  ***************************************************************
// by using querySelector we can use our css Selectors directly
// like in css if we have to style on a class or id so we directly select it here also we do it
// but querySelector selects only one value 

document.querySelector("title")
<title>​DOM​</title>​

document.querySelector("ul")
<ul>​…​</ul>​
const myUl = document.querySelector("ul")
undefined
myUl.querySelector("li")
<li>​…​</li>​
const tunGreen = myUl.querySelector("li")
undefined
tunGreen.style.backgroundColor="red"
'red'

//*************************************    qerySelectorAll *****************************************************


querySelector only selects the first item 
like in document.querySelector("li")
it will select olny first list item


document.querySelectorAll("li")
NodeList(3) [li, li, li]

but document.querySelectorAll returns a node list 


// ** imp  point : NodeList and HTMLcollections are not array or hum bol skte hai ki ye pure array nhi hai 

document.querySelectorAll("li")
NodeList(3) [li, li, li]
const myLI = document.querySelectorAll("li")
undefined
myLI[2].style.color = "yellow"
'yellow'
document.querySelectorAll("h1")
NodeList [h1#title.heading]
const heading = document.querySelectorAll("h1")
undefined
heading.style.color = "green"
VM8066:1 Uncaught TypeError: Cannot set properties of undefined (setting 'color')
    at <anonymous>:1:21
(anonymous) @ VM8066:1

// if we want to perform something on node list then we have to give its index position also wether there is only element of that name in document

// if we dont give index positon while accessing the node list it will give us error

// we can use some method of arrays in nodelist as nodelist is not pure array , so we can only use some methods

entries
: 
ƒ entries()
forEach
: 
ƒ forEach()
item
: 
ƒ item()
keys
: 
ƒ keys()
length
: 
(...)
values
: 
ƒ values()
constructor
: 
ƒ NodeList()
Symbol(Symbol.iterator)
: 
ƒ values()
Symbol(Symbol.toStringTag)
: 
"NodeList"
get length
: 
ƒ length()



